apiVersion: v1
kind: Service
metadata:
  name: {{ include "powderandcanvas.fullname" . }}
  labels:
    {{- include "powderandcanvas.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "powderandcanvas.selectorLabels" . | nindent 4 }}
