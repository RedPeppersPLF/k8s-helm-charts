apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: grafana-postgres-external-secret
  namespace: cnpg
spec:
  refreshInterval: "15s"
  secretStoreRef:
    name: vault-secret-store
    kind: ClusterSecretStore
  target:
    name: grafana-postgres-credentials
  data:
  - secretKey: username
    remoteRef:
      key: secrets/grafana/postgres
      property: username
  - secretKey: password
    remoteRef:
      key: secrets/grafana/postgres
      property: password